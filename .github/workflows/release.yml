name: Release

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  release:
    uses: sebastian-software/ferramenta/.github/workflows/release-rust.yml@main
    permissions:
      contents: write
      issues: write
      pull-requests: write
    secrets: inherit
